
import java.io.IOException;


public class Main {
    public static void main(String[] args) throws IOException {
       /* Path path = Paths.get("src/main/resources/conferences.json");
        BufferedReader is = Files.newBufferedReader(path);
        StringBuilder stringBuilder =  new StringBuilder();
        JsonParser jsonParser =  new com.google.gson.JsonParser();
        Gson gson = new GsonBuilder().setDateFormat("yyyy-MM-dd'T'HH:mm:ss").create();

        Conference[] conferences = gson.fromJson(is,Conference[].class);
        int a = 2;*/
    }
}
